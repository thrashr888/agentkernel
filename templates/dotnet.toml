# .NET sandbox template
# .NET SDK for C# / F# development

[sandbox]
name = "dotnet"
base_image = "mcr.microsoft.com/dotnet/sdk:8.0"

[resources]
vcpus = 2
memory_mb = 1024

[security]
profile = "moderate"

[security.domains]
allow = ["api.nuget.org"]
