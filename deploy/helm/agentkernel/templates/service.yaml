apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentkernel.fullname" . }}
  labels:
    {{- include "agentkernel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentkernel.selectorLabels" . | nindent 4 }}
