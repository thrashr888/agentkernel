{{- if .Values.apiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agentkernel.fullname" . }}-api-key
  labels:
    {{- include "agentkernel.labels" . | nindent 4 }}
type: Opaque
stringData:
  AGENTKERNEL_API_KEY: {{ .Values.apiKey | quote }}
{{- end }}
